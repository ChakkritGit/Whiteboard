# The room server only. The site is built and hosted separately; see the README.
FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY scripts ./scripts
ENV PORT=1234
EXPOSE 1234
CMD ["node", "scripts/server.mjs"]
